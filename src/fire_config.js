import * as constants from './constants';

export const fire_config = {
    apiKey: constants.FIRE_API_KEY,
    authDomain: constants.FIRE_AUTH_DOMAIN,
    databaseURL: constants.FIRE_DB_URL,
    storageBucket: constants.FIRE_STORAGE_BUCKET,
    messagingSenderId: constants.FIRE_MSG_SENDER_ID
};

export default fire_config;